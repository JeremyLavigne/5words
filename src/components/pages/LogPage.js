function LogPage({ handleClickLog }) {
  return (
    <main>
      Log Page
      <button className="btn btn-login" onClick={handleClickLog}>
        Log
      </button>
    </main>
  );
}

export default LogPage;
